## Setup
1. Run `cmake -B build -S ./` in project folder
2. To run project, run `cmake --build build --target flash_lab1` in project folder
3. To use USART, connect RX to PB10, TX to PB11, and GND to GND. Baud rate is 115200.
## References and credits
FT8 library provided by https://github.com/kgoba/ft8_lib